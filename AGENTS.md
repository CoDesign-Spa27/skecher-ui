# Workspace instructions

- Never run a build command in this repository.
- Never start a development server in this repository.
- Validate changes with non-build checks only, such as targeted linting, type checking, and static inspection.
